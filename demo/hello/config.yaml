# Configuração do Comando
# Referência: https://duducp.github.io/susa/guides/adding-commands/

name: "Hello World"
description: "Exibe uma mensagem de saudação personalizada"
entrypoint: "main.sh"
sudo: false
os: ["linux", "mac"]

# Arquivos .env (opcional)
# Descomente para carregar variáveis de arquivos
# env_files:
#   - ".env"
#   - ".env.local"

# Variáveis de ambiente do comando
envs:
  # Prefixo da mensagem
  HELLO_PREFIX: "👋"

  # Mensagem padrão
  HELLO_MESSAGE: "Hello"

  # Cor da mensagem (green, blue, yellow, red)
  HELLO_COLOR: "green"
